{
  "name": "rapidresq",
  "version": "1.0.0",
  "private": true,
  "description": "RapidResq — emergency response web platform (React + Express API)",
  "scripts": {
    "install:all": "npm install --prefix web && npm install --prefix backend",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm start --prefix web",
    "build:web": "npm run build --prefix web",
    "start:api": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=18.x"
  }
}
